# swag2api

**Swag2API** uses Go annotations supported by the [**swag**](https://github.com/swaggo/swag) tool and Go function definitions to automatically build simple API for HTTP requests. It alllows you to use common Go functions as HTTP handlers by just adding proper annotations to it.
